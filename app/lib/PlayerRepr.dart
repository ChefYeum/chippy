import 'package:flutter/material.dart';

class PlayerRepr extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
        mainAxisAlignment: MainAxisAlignment.start,
        children: [Text("SomePlayerName"), Text("123400")]);
  }
}
